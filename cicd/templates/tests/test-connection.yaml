apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-wif.fullname" . }}-test-connection"
  namespace: {{ include "chart-wif.namespace" . }}
  labels:
    {{- include "chart-wif.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-wif.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
