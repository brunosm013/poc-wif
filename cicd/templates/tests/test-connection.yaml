apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "char-wif.fullname" . }}-test-connection"
  namespace: {{ include "char-wif.namespace" . }}
  labels:
    {{- include "char-wif.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "char-wif.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
